name: facipoint_sunmi_printer_example
description: Demonstrates how to use the facipoint_sunmi_printer plugin.

publish_to: "none"

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  facipoint_sunmi_printer:
    path: ../
  fluttertoast: ^8.2.4
  intl: ^0.18.0
  characters: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/
